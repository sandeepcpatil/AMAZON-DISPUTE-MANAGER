# Amazon Disputes Manager

Django + HTMX app to manage Amazon disputes, orders, and returns.  

## Run
```bash
git clone https://github.com/sandeepcpatil/AMAZON-DISPUTE-MANAGER.git && cd amazon-disputes && docker compose up --build
